import React from "react";
import { View } from "react-native";
import { globalStyles } from "@/src/globalstyles";

export default function PageFiller() {
  return <View style={globalStyles.pageFiller} />;
}
